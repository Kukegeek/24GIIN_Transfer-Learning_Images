Model: model03 (10_samples_21-30.keras)

Descripción:
- Este modelo fue generado con 10 imágenes por clase (muestras 21-30).
- Imágenes de entrenamiento por clase tomadas desde `training/images/01_soup`, `training/images/02_main`, `training/images/03_salad`, `training/images/04_dessert`, `training/images/05_nofood`.
- Contiene `keras_model.h5` y `labels.txt`.

Uso:
- Coloca esta carpeta dentro de `training/models/` y ejecuta `python evaluate_models.py` (o añade `model03` a `MODEL_FOLDERS`).

---

English

Model: model03 (10_samples_21-30.keras)

Description:
- This model was generated with 10 images per class (samples 21-30).
- Training images per class are taken from `training/images/01_soup`, `training/images/02_main`, `training/images/03_salad`, `training/images/04_dessert`, `training/images/05_nofood`.
- Contains `keras_model.h5` and `labels.txt`.

Usage:
- Place this folder inside `training/models/` and run `python evaluate_models.py` (or add `model03` to `MODEL_FOLDERS`).
